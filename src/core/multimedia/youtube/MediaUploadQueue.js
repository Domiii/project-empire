

export default class MediaUploadQueue {
  /**
   * 
   */
  fileInfos;

  /**
   * Index of the video that is currently being uploaded
   */
  uploadingIndex;
  constructor(fileInfos, cfg) {
    this.fileInfos = fileInfos;
    this.cfg = cfg;

    //this.
  }
  
  

}